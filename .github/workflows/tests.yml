name: tests

on:
  push:
    branches: [main]
  pull_request:

jobs:
  full:
    runs-on: macos-15
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
      - run: ./scripts/verify.sh
