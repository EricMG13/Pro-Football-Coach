.DS_Store
.build/
DerivedData/
*.xcodeproj/
*.xcworkspace/
xcuserdata/
*.xcuserstate
.swiftpm/
.worktrees/
exports/Claude-Design-Team-Logo-Handoff-2026-08-20.zip

# Xcode build products. These were committed once (70 MB) and went stale:
# the last compiled module predates Sources/FootballSimCore/Arcade/ entirely.
build/

# An accidental gitlink pointed here: a nested clone of this same repo was staged as a
# submodule (mode 160000) with no .gitmodules, so clones got an uninitialisable empty directory.
Pro-Football-Coach/

# Owner-supplied competitor reference screenshots. Third-party copyright:
# research material only, never redistributed. See CLAUDE.md legal guardrail.
FM Screenshots/
