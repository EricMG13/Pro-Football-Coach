# Future Simulation Contract

This is the repository-local register for promises that cannot yet be implemented truthfully.
Entries become active only when their named dependency owns authoritative state. A UI placeholder,
random value, or prose-only event does not satisfy an entry.

| ID | Required future behavior | Missing dependency | Activation milestone |
|---|---|---|---|
| FSC-001 | **Activated in M1.** Validate every hot domain-event entity reference against the authoritative root. | M1 game, postseason, and season-completion payloads carry organisation IDs; `eventReferences` is active. Historical game IDs remain event identities rather than normalized entity references. | Complete for current payloads; extend with each new payload. |
| FSC-002 | Persist archived event bodies and guarantee event-ID uniqueness across hot and cold history. | M0 has a bounded hot ledger and archive accounting, but no cold-storage persistence layer. M2 retains compact departed identities/careers, not cold event bodies. | Persistence/storage milestone. |
| FSC-003 | Compress and, if measurements require it, chunk/stream snapshots; reject oversized imports before allocating their complete body. | M2's honest uncompressed snapshots measured 22.12 MB at season 1, 35.26 MB at season 5, and 84.66 MB at season 20. `SaveEnvelope` has header validation but no compression, streaming import, or final product budget enforcement. | Persistence and performance milestone. |
| FSC-004 | **Activated in M1.** Validate schedule references/results, regular-season standings, statistics, bracket fields, prior-round winners, champions, compact archives, and record context. | Current M1 competition state is authoritative and its projections are rebuilt from results. | Complete for M1; extend when later competition formats are added. |
| FSC-005 | **Activated in M3.** Eligibility, exact scholarship ownership, prospect cycles, scouting, recruiting relationships, commitment capacity/conservation, recorded game participation, usage-aware redshirt plans/outcomes, one unified roster/recruiting/portal NIL ledger, schema-6 portal-window records, authoritative intent, exact retention, coverage-aware deterministic matching, atomic transfer execution, typed history, whole-root portal integrity, and both scheduled portal windows are checked. | Current college lifecycle, recruiting, redshirt, NIL, portal, and walk-on boundaries are authoritative; extend the same pattern when later tiers add movement rules. | Complete for the M3 college ecosystem; extend with later roster systems. |
| FSC-006 | **Partially activated in M6.** Cap snapshots, atomic professional acquisitions/releases, contract validation, signing-bonus dead money, and guarded promoted-coach intents are live. Contract expiry, draft rights, dated free agency, trades, and practice-squad rules remain. | The existing `Player.contract`/`ProTeam` model now owns the first transaction boundary; dated market state and professional actor/UI do not yet exist. | Complete in later M6 slices. |
| FSC-007 | **Activated for college prospects and portal players in M3.** Observer-specific observations are separate from hidden truth; sealed portal snapshots produce deterministic, immutable, bounded knowledge without permitting repeated resampling. Opponent and draft knowledge remain open. | Later tactical/pro milestones must apply the same permitted-knowledge boundary to opponent preparation and the professional draft. | Complete across M4 and M6. |
| FSC-008 | **Partially activated through M2.** Validate minimum roster coverage after every lifecycle transition. Tactical package and role eligibility remain unavailable because depth charts and role assignments do not exist. | M1/M2 own base and rollover coverage; tactics milestone owns the remainder. |
| FSC-012 | **College half activated in M3.** College departures now resolve through signed recruits plus explicit lower-rated walk-ons; no college provisional replacement remains. | Professional rosters still need M6 draft/free agency, so the pro bridge remains. | Complete in M6. |
| FSC-009 | **Activated in M3.** Deterministic recruiting AI and delegated controlled-programme recruiting use the same legal action engine as explicit career intents. Persisted responsibility ownership, typed mandatory decisions and resolutions, stable option identities, causal reasons, deadlines, recommendations, actor-owned immutable projections, cancellation-safe commits, and the no-UI-root boundary are authoritative. | Later management systems must extend the same authority and exception pattern rather than bypassing the career session. | Complete for the M3 college ecosystem; extend with later management systems. |
| FSC-010 | Produce historical entity-linked news, rivalries, records, staff trees, and career memories from events. | M0 stores typed history but later systems do not yet emit their domain facts or build projections. | Each emitting system, completed by history/career milestone. |
| FSC-011 | Render matches from recorded movement/decision anchors without resolving football in the UI. | Existing match records explain outcomes but do not yet contain the full top-down animation anchor stream. | Match-presentation milestone after anchor contract. |
| FSC-013 | Validate every recorded participant against the roster they legally belonged to at the game's season and week after in-season movement is enabled. | M3 safely validates against current rosters because ownership is stable while current-season results remain live. Dated roster-tenure/transaction history does not yet exist, so the same check would be untruthful after midseason trades or other in-season moves. | Activate with the first in-season roster-movement system, no later than professional trades. |
| FSC-014 | Activate the mechanical effects and deterministic population of `ironman`, `workhorse`, `iceInVeins`, `frontRunner`, `mentor`, `adaptable`, and `volatile`. | M3 portal intent now needs and populates `restless`, whose retention effect is live. The other trait names identify future injury, development, match, scheme, and discipline dependencies, but those systems do not yet consume them; production generation therefore must not assign them as decorative labels. | Activate each trait with its named authoritative system across M4–M6. |

## Scheduler activation at the current M3 checkpoint

M3 now additionally executes `scoutingKnowledge`, college `marketInteractions`, and college
`aiDecisions`; each has focused ordering, event, determinism, and integrity tests. The detailed user
game, relationships/stakeholders, news/narrative, pro markets, and later systems remain visibly
inactive in their fixed positions. Activation still requires a focused proof of inputs, events,
determinism, and downstream integrity.
